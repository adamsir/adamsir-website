import { ProfileCard } from '#/components/profile-card';

export default function Home() {
  return (
    <main className="max-w-lg mx-auto p-4 flex items-center h-screen">
      <ProfileCard />
    </main>
  );
}
